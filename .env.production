# Placeholder — set to the real deployed CryptoPulse_BackendDemo API origin
# once its subdomain/hosting is finalized (see the backend repo's README for
# the outstanding server-side deploy steps). CORS_ALLOW_ORIGIN on the backend
# must also be updated to match this frontend's real deployed origin.
VITE_API_BASE_URL=https://crypto-pulse-api.example.invalid
